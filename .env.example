# =============================================================================
# Reservation Service Environment Configuration
# =============================================================================
# Copy this file to .env and fill in the values for your environment

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# PostgreSQL Database Credentials
POSTGRES_USER=reservation_service
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=reservation_db

# Complete Database Connection URL
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://reservation_service:your_secure_password_here@localhost:5432/reservation_db

# Enable SQL query logging (development only)
SQL_DEBUG=false

# =============================================================================
# KEYCLOAK AUTHENTICATION CONFIGURATION
# =============================================================================

# Keycloak Server Configuration
KEYCLOAK_URL=https://keycloak.parkora.crn.si/auth/
KEYCLOAK_REALM=parkora
KEYCLOAK_CLIENT_ID=backend-services
KEYCLOAK_CLIENT_SECRET=your-keycloak-client-secret-here

# =============================================================================
# EXTERNAL SERVICES CONFIGURATION
# =============================================================================

# Parking Service (HTTP)
PARKING_SERVICE_URL=https://parkora.crn.si/api/v1/parking
PARKING_SERVICE_TIMEOUT=10.0

# Payment Service (gRPC)
PAYMENT_SERVICE_HOST=payment-service
PAYMENT_SERVICE_PORT=50051

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Application Settings (Optional - defaults will be used if not set)
# APP_ENV=development  # Options: development, staging, production
# LOG_LEVEL=INFO       # Options: DEBUG, INFO, WARNING, ERROR

# =============================================================================
# DEVELOPMENT NOTES
# =============================================================================
# 1. Never commit the .env file to version control
# 2. Use strong, unique passwords for database and Keycloak secrets
# 3. For local development, you can use:
#    - PostgreSQL running locally or in Docker
#    - Keycloak instance (local or remote)
# 4. For production, use environment-specific values and secret management
# 5. In Kubernetes, secrets are injected via Azure Key Vault CSI Driver
